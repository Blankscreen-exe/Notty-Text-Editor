<Window x:Class="Notty.App.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notty" Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" FontFamily="Segoe UI" FontSize="13"
        Background="{DynamicResource WindowBackgroundBrush}">
    <StackPanel Margin="18">
        <TextBlock x:Name="PromptText" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,8" />
        <TextBox x:Name="Input" Padding="6,4" KeyDown="Input_KeyDown" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Style="{StaticResource ToolbarButton}" Padding="16,6"
                    IsCancel="True" Click="Cancel_Click" />
            <Button Content="OK" Style="{StaticResource PrimaryButton}" Margin="6,0,0,0"
                    IsDefault="True" Click="Ok_Click" />
        </StackPanel>
    </StackPanel>
</Window>
